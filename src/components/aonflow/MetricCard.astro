---
interface Props {
  label: string;
  value: string;
}

const { label, value } = Astro.props;
---

<article class="metric-card">
  <p class="text-xs uppercase tracking-[0.24em] text-cyan-300">{label}</p>
  <p class="mt-3 text-lg font-semibold text-white">{value}</p>
</article>
