---
interface Props {
  label: string;
  value: string;
}

const { label, value } = Astro.props;
---

<article class="metric-card">
  <p class="text-xs uppercase tracking-[0.24em] text-accent-text">{label}</p>
  <p class="mt-3 text-lg font-semibold text-foreground">{value}</p>
</article>
